name: Demo ci Env secrets inputs

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, world!
      - name: Install
        env:
          App_Env: demo
        run: echo Eniviroment ${App_Env}