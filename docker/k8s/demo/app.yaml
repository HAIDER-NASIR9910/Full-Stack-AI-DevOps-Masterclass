apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: nginx
        image: my-app:2.0
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: my-config
        - secretRef:
            name: my-secret